{% extends 'base.html.twig' %}

{% block title %}
    add answer
{% endblock %}

{% block body %}
    <div class="card mt-5">
        <div class="card-body">
            <div class="card-title">dodaj odpowiedz</div>

            {{ form_start(form, { method: 'POST', action: url('add_answer', {"id": id}) }) }}
            {{ form_widget(form) }}
            <div class="form-group row float-sm-right">
                <input type="submit" value="zapisz" class="btn btn-primary" />
            </div>
            {{ form_end(form) }}

        </div>
    </div>

{% endblock %}
